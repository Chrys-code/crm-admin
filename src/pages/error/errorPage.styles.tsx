import styled from 'styled-components';
import { ErrorPageContainerStyleProps } from './errorPage.types';

export const ErrorPageContainer = styled.div<ErrorPageContainerStyleProps>`
  ${({ theme }) => ({})}
`;
