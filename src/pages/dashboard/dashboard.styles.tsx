import styled from 'styled-components';
import { DashboardContainerStyleProps } from './dashboard.types';

export const DashboardContainer = styled.div<DashboardContainerStyleProps>`
  ${({ theme }) => ({})}
`;
