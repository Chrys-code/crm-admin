import styled from 'styled-components';
import { ProfileContainerStyleProps } from './profile.types';

export const ProfileContainer = styled.div<ProfileContainerStyleProps>`
  ${({ theme }) => ({})}
`;
