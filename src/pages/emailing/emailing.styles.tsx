import styled from 'styled-components';
import { EmailingContainerStyleProps } from './emailing.types';

export const EmailingContainer = styled.div<EmailingContainerStyleProps>`
  ${({ theme }) => ({})}
`;
