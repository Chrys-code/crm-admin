import styled from 'styled-components'

export const OrganisationInfoWrapper = styled.div`
    ${({ theme }) => ({
        width: '100%',
        display: 'flex',
        flexDirection: 'column'
    })}
`
