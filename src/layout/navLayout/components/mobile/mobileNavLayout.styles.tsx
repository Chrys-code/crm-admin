import styled from 'styled-components';
import { MobileNavLayoutContainerStyleProps } from './mobileNavLayout.types';

export const MobileNavLayoutContainer = styled.div<MobileNavLayoutContainerStyleProps>`
  ${({ theme }) => ({})}
`;
