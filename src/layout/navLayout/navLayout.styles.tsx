import styled from 'styled-components';
import { NavLayoutContainerStyleProps } from './navLayout.types';

export const NavLayoutContainer = styled.div<NavLayoutContainerStyleProps>`
  ${({ theme }) => ({})}
`;
