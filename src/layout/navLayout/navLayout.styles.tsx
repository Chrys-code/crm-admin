import styled from 'styled-components';
import { NavLayoutContainerStyleProps } from './navLayout.types';

export const NavLayoutContainer = styled.header<NavLayoutContainerStyleProps>`
  ${({ theme }) => ({})}
`;

export const Nav = styled.nav`
  ${({ theme }) => ({})}
`;
