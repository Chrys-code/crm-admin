import { ListItemType } from '../../list.types';
import { Location } from 'react-router-dom';

export interface LinkProps {
  item: ListItemType;
  location: Location;
}
export interface LinkContainerStyleProps {}
